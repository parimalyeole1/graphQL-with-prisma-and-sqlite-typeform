type Query {
  artists: [Artist!]!
}

type Mutation {
  updateArtist(name: String!): Artist!
}

# type Subscription {
# }

type Artist {
  artistId: ID!
  name: String!
  albums: [Album!]!
}

type Album {
  albumId: ID!
  title: String!
  artistId: ID!
}